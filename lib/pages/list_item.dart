import 'package:flutter/material.dart';

class ListItemPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Material(
        child:
            Container(child: Image.asset('assets/images/logo_mart9_bag.png')),
      ),
    );
  }
}
